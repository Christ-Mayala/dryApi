Je vais transformer `dryApp` en un framework frontend de niveau entreprise, documenté et modulaire, parfaitement aligné avec la philosophie `dryApi`.

### 1. 🏗️ Refonte Architecturale (Le "Framework")
Je vais restructurer le code pour séparer clairement le **Noyau (Framework)** du **Code Métier (App)**.
*   **Création de `src/app/@dry/`** : Ce sera le cœur intouchable (comme le dossier `dry/` du backend).
    *   `@dry/core` : Services singletons (Auth, Config, Log, ErrorHandler).
    *   `@dry/ui` : La bibliothèque de composants visuels (Buttons, Cards, Tables, Inputs).
    *   `@dry/modules` : Les "briques Lego" fonctionnelles réutilisables (Chat, Admin, Profile, Notifications).
    *   `@dry/sdk` : La couche de communication API (Générée ou Manuelle).
*   **Migration** : Je vais déplacer `auth`, `users`, `settings` (qui sont génériques) vers `@dry/modules` pour les rendre réutilisables par n'importe quel projet.
*   **Features** : Le dossier `src/app/features` sera désormais réservé **uniquement** aux spécificités de votre projet (ex: `products`, `orders`).

### 2. 📚 Documentation "Entreprise"
Je vais créer un dossier `docs/` complet à la racine, structuré exactement comme celui de l'API :
*   `01_GETTING_STARTED.md` : Installation et premier lancement.
*   `02_DEVELOPER_GUIDE.md` : Comment créer une page, utiliser le CLI, architecture.
*   `03_ARCHITECTURE.md` : Explication du pattern `@dry` vs `features`.
*   `04_COMPONENTS.md` : Catalogue des composants UI (Button, Card, Table...).
*   `05_MODULES.md` : Doc des modules intégrés (Chat, Auth...).

### 3. 💬 Module Chat & Notifications
Je vais finaliser le système de messagerie pour en faire un module "plug-and-play" :
*   **Chat** : Interface de discussion temps réel (Rooms, Messages privés).
*   **Notifications** : Centre de notifs amélioré avec historique et badges.
*   Ce module sera dans `@dry/modules/chat` et pourra être activé/désactivé via la config.

### 4. 🛠️ Le Générateur (Scaffolding)
Je vais créer un script intelligent `scripts/generate.js` (similaire à celui de l'API) qui permettra de :
*   `npm run generate page <nom>` : Crée une page vide avec la structure standard.
*   `npm run generate dashboard` : Scaffolde un layout dashboard complet.
*   `npm run generate crud <feature>` : Génère les listes/formulaires basés sur l'API.

### Plan d'exécution
1.  **Architecture** : Création des dossiers `@dry` et déplacement des modules core (`auth`, `users`, `settings`).
2.  **Documentation** : Rédaction des 5 fichiers de documentation.
3.  **Chat** : Implémentation du module Chat.
4.  **Generator** : Création du script de scaffolding.

Une fois terminé, `dryApp` ne sera plus juste une "app", mais une **usine à sites web**, capable de sortir un Dashboard Admin ou un Site Vitrine en quelques commandes, avec toute la plomberie (Auth, Chat, UI) déjà posée.