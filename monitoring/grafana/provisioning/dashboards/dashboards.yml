apiVersion: 1

providers:
  - name: 'DRY API'
    orgId: 1
    folder: 'DRY API'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
